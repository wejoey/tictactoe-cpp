{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cmake build debug",
      "type": "shell",
      "command": "cmake -B build -G Ninja -D CMAKE_BUILD_TYPE=Debug && cmake --build build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
    }
  ]
}
