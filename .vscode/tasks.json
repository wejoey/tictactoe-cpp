{
  // https://code.visualstudio.com/docs/editor/variables-reference
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cmake build debug",
      "type": "shell",
      "command": "cmake -B debug -G Ninja && cmake --build debug",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
    }
  ]
}
